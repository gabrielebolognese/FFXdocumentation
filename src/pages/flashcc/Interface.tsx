import Layout from '../../components/Layout';
import SEO from '../../components/SEO';

export default function FlashCCInterface() {
  return (
    <Layout>
      <SEO
        title="The interface in FlashCC"
        description="The interface in FlashCC, the companion app to FlashFX for creating carousels."
      />
      <div>
        <h1 className="text-4xl font-semibold text-white mb-4">The interface</h1>
        <p className="text-sm text-white">Content will appear here</p>
      </div>
    </Layout>
  );
}
